language: objective-c
before_install: gem install xcpretty slather -N
script:
- set -o pipefail && xcodebuild -workspace objcTox.xcworkspace -scheme objcTox -sdk iphonesimulator GCC_INSTRUMENT_PROGRAM_FLOW_ARCS=YES GCC_GENERATE_TEST_COVERAGE_FILES=YES clean test | xcpretty -c
# - pod lib lint --quick
after_success: slather

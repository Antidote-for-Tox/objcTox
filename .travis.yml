language: objective-c
osx_image: xcode7.3

before_install:
  - gem install cocoapods -v 1.0.1
  - brew install uncrustify

install:
  - pod _1.0.1_ install

env:
  - ENV=iOS
  - ENV=macOS
  - ENV=uncrustify

script:
  - ./run-ci.sh $ENV
